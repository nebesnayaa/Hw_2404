1) встановити бібліотеку axios, використовувати ії для запиту на сервер
https://www.npmjs.com/package/axios
2) для users реалізувати наступні функції:
зміна даних (login, пароль - PUT запит на сервер). Для цього потрібно створити форму
3) Додати у форму поле select(роль користувача: admin, manager, user) та checkbox(пол: чоловік, жінка)
та checkbox(статус: enable/disable)
PS: проект у тг, якщо бажаєте можна робити запита не до json-server а до ASP.